query GetRecipes($ingredients: [String!]) {
  recipes(ingredients: $ingredients) {
    nodes {
      id
      title
      ratings
      author
      ingredients {
        description
      }
      category {
        name
      }
      image
      createdAt
      updatedAt
    }
  }
}
